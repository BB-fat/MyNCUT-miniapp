<import src="../../templates/myButton/myButton.wxml" />

<view class='mainView'>
  <view class="card">
    <view class="cardView">
      <view class="cardViewLeft">
        <open-data type='userAvatarUrl' class="card-avata"></open-data>
      </view>
      <view class="cardViewMid">
        <view class='row0'>
          <text class="card-name">{{userInfo.name}}</text>
          <image wx:if='{{userInfo.sex==1}}' src='/img/my/male.png' class="card-male"></image>
          <image wx:else src='/img/my/female.png' class="card-male"></image>
        </view>
        <text class="card-userid">{{userInfo.userid}}</text>
        <text class="card-class">{{userInfo.class}}</text>
      </view>
    </view>
  </view>
  <view class='wifiView' bindtap='flowTap'>
      <image src='/img/my/wifi.svg'></image>
      <text class='tips'>校网流量</text>
      <text wx:if="{{wifiLeft>0}}" class='leftWifi'>剩余{{wifiLeft}}GB</text>
      <text wx:else class='leftWifi'>已使用超过30G</text>
      <progress class='wifiProgress' percent="{{wifiProgress}}" active active-mode="backwards" activeColor="#337DEF" border-radius='9' stroke-width='20' font-size='14'></progress>
    </view>
  <view class="button-area">
    <view class='row1'>
      <template is="myButton" data="{{...btn2}}" />
      <template is="myButton" data="{{...btn1}}" />
      <template is="myButton" data="{{...btn3}}" />
      <template is="myButton" data="{{...btn5}}" />
      <template is="myButton" data="{{...btn4}}" />
    </view>
    <image class='picture' bindtap="seePic" src="/img/my/schoolGate.svg"></image>
  </view>
</view>