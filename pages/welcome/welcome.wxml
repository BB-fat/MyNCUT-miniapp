<import src="../../templates/we_button/we_button.wxml" />

<view class='banner-bd'>
  <swiper circular='true' autoplay='true' duration='1000' indicator-active-color='white' interval='5000'
    style='height:400rpx;'>
    <swiper-item wx:for="{{banner}}">
      <image src='{{item.image}}' mode='widthFix' class='swiper-image' data-url="{{item.url}}" bindtap='jump'></image>
    </swiper-item>
  </swiper>
</view>

<view class='b'>
  <view class='big'>新生必备</view>
  <view class='a' style="width:80%">
    <template is="we_button" data="{{...zhengjian}}" />
    <template is="we_button" data="{{...ruzhu}}" />
    <template is="we_button" data="{{...xuexi}}" />
  </view>

  <view class='big'>学校概况</view>
  <view class='a' style='width:80%'>
    <template is="we_button" data="{{...gaikuang}}" />
    <template is="we_button" data="{{...ditu}}" />
  </view>

  <view class='big'>学生组织</view>
  <view class='a' style='width:80%'>
    <template is="we_button2" data="{{...zuzhi}}" />
  </view>

  <view class='big'>校园生活</view>
  <view class='a' style='width:80%'>
    <template is="we_button" data="{{...shitang}}" />
    <template is="we_button" data="{{...gongyu}}" />
    <template is="we_button" data="{{...fuwu}}" />
  </view>
  <view class='a' style='width:80%'>
    <template is="we_button" data="{{...jiaotong}}" />
    <template is="we_button" data="{{...tushuguan}}" />
    <template is="we_button" data="{{...tiyuguan}}" />
  </view>

  <view class='big'>校园周边</view>

  <view class='a' style='width:80%'>
    <template is="we_button" data="{{...chihe}}" />
    <template is="we_button" data="{{...wanle}}" />
  </view>
  <view class='a' style='width:80%'>
    <template is="we_button" data="{{...jianshen}}" />
    <template is="we_button" data="{{...yiliao}}" />
  </view>


  <view class='big'>精品活动</view>
  <view class='a' style="width:80%">
    <template is="we_button2" data="{{...huodong}}" />
  </view>
  <text class="rights">OneTech. All Rights Reserved.</text>
  <view style="height:40rpx;"></view>
</view>